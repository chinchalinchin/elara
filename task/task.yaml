task:
# ------------------------------------------------
  context: true
# ------------------------------------------------
  system: 
    guidelines: 
      coding: true
# ------------------------------------------------
  ticket: 
    # -------------------------------------------
    title: "Ontology: Rendering"
    # -------------------------------------------
    content: |
      Explain the SDL flow. It seems like there needs to be a function to crop the rendered image so that it is centered on the player, except in the cases where the player is approaching the edges of the board. 
    # -------------------------------------------
    shell: []
# ------------------------------------------------
  tools:
    # -------------------------------------------
    github: 
      - parameters:
          repo: ontology
          group: chinchalinchin
        views:
          directory: true
          ontology/docs: true
          ontology/board: false
          ontology/app/assets: true
          ontology/app/game: true
          ontology/app/main: true
          ontology/app/models: true
          ontology/app/input: true
          ontology/cython: true