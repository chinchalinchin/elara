task:
# ------------------------------------------------
  context: true
# ------------------------------------------------
  system: 
    guidelines: 
      coding: true
      latex: false
      pronouns: false
      notation: false
      interaction: false
    functions:
      linguistic: false
    procedures:
      poetics: false
# ------------------------------------------------
  ticket: 
    # -------------------------------------------
    title: "Elara"
    # -------------------------------------------
    content: |
      If this can be done through the conf.py, do that. If not, override the appropriate template.

      - Remove the <footer> which has the "Next" and "Previous" buttons at the bottom of the page.
      - Remove the "dark/light" toggle. Website should always be in dark mode. 
    # -------------------------------------------
    shell: 
      - cat /Users/gmoore/Home/projects/elara/build/html/index.html
# ------------------------------------------------
  tools:
    # -------------------------------------------
    github: 
      - parameters:
          repo: elara
          group: chinchalinchin
        views:
          directory: true
          elara_build: true
          elara_linguistics: false
          elara_extensions: true
          elara_styles: true