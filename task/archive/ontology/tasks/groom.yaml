task:
# ------------------------------------------------
  context: true
# ------------------------------------------------
  system: null
# ------------------------------------------------
  ticket: 
    # -------------------------------------------
    title: "Ontology: Groom"
    # -------------------------------------------
    content: |
      Discuss the design and architecture of this application with respect to the current phase of the Task Board. Do not implement anything; rather step back and examine the big picture. Focus on the logical flow of the application. Suggest improvements, if applicable. Discuss what needs modified in the task backlog, if changes are required. Ensure tasking aligns with architecture. Break everything down, step-by-step.
      
      Discuss the finite automata implied by the Intention Transition
      **Grooming Guidelines**

      - Create tasks in the Task Board to achieve the next Phase of the application. Lay out, step-by-step, what must be done.
      - Keep in mind, this is a 2D game engine that is cropping pre-rendered assets on the fly, not rendering fully-textured ray-traced 3D graphics from scratch. 
      - Do not suggest C-level optimizations at this stage of the application.
      - Do not worry about Python GIL locks yet.

    # -------------------------------------------
    shell: []
# ------------------------------------------------
  tools:
    # -------------------------------------------
    github: 
      - parameters:
          repo: ontology
          group: chinchalinchin
          board: 
            phase: iv
            action: implement
        views:
          directory: true
          board: true
          docs: true
          python: false
          ontology/app/main: false
          ontology/app/input: true
          ontology/app/config: true
          ontology/app/models: true
          ontology/app/hooks: true
          ontology/app/game: true
          ontology/app/assets: true
          ontology/cython/core: false
          ontology/cython/graphics: false
          ontology/schemas/assets: true
          ontology/schemas/intents: true
          ontology/schemas/items: true
          ontology/schemas/player: true