{% for task_data in task.tools.jenkins if task_data.views.config %}
{% set url = 'https://' + properties.tools.jenkins.url + '/job/' + task_data.parameters.job + '/config.xml' %}
### View: Build Configuration

```bash
{{ properties.inventory.local.username }}@localhost % curl \
    -u "$JENKINS_USERNAME:$JENKINS_PASSWORD" \
    {{ url }}

{{ command('curl -u "$JENKINS_USERNAME:$JENKINS_PASSWORD" ' + url) }}
```
{% endfor %}